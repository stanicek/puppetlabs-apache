<% if @fpm_server -%>

  #FastCgiExternalServer < %= @fastcgi_server % > -socket < %= @fastcgi_socket % >
  #FastCgiExternalServer /var/www/external-php-wrapper.fcgi -socket /var/run/fpm-< %= name % >.socket -user < %= name % > -group users -idle-timeout < %= idle_timeout % > -pass-header Authorization
  Alias /php5-fcgi <%= @fpm_server %>
  #FastCgiExternalServer <%= @fpm_server %> -socket <%= @fpm_socket %> -user <%= @fpm_user %> -group <%= @fpm_group %>
  FastCgiExternalServer <%= @fpm_server %> -host 127.0.0.1:9000 -user <%= @fpm_user %> -group <%= @fpm_group %>
  
<% end -%>